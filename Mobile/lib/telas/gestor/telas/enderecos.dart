import 'package:flutter/material.dart';

class DesempenhoTab extends StatelessWidget {
  const DesempenhoTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("📈 Desempenho da Equipe"));
  }
}
