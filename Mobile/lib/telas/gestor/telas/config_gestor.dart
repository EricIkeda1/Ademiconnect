import 'package:flutter/material.dart';

class ConfigGestor extends StatelessWidget {
  const ConfigGestor({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("⚙ Configurações do Gestor"));
  }
}
