import 'package:flutter/material.dart';

class ColaboradoresTab extends StatelessWidget {
  const ColaboradoresTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("👥 Colaboradores"));
  }
}
